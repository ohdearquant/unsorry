# CI Failure Triage

Check: `<gate-a/gate-b/other>`
Run URL or commit: `<identifier>`
Touched paths:
- `<path>`

First failing step:
- `<step name>`

Observed error:
```text
<short excerpt>
```

Likely class:
- `<immutability/binding/build/axiom/replay/AISP/generated-doc/provider/protocol>`

Local reproduction:
```bash
<command>
```

Fix plan:
- `<next action>`
