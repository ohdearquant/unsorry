Gate A gains a SPEC-049-A §5 conformance regression suite (`tools/gate_a/tests/test_decentralised_runner_conformance.py`) that locks in the ADR-049 decentralised-runner soundness invariant: the central re-check feeds `leanchecker` only locally-derived module names (never a contributor-supplied `.olean`/artifact), an untrusted diff fails closed to a full re-check, a changed module always drags its ADR-011 `*Binding` and reverse-import closure into scope (the PR-#64 vacuity class), a global-impact change forces a full re-check, and `gate-a.yml` feeds no downloaded artifact into the central build/replay. This is the ADR-049 Phase-0 deliverable (issue #635) — the guard that must exist before any Phase-1 change narrows the central elaboration build; the Phase-1-dependent items (cache provenance §5.4, determinism §5.5) are recorded as skipped placeholders.
