Research deliverable for decentralised CI runner architecture (issue #635): a ranked options analysis (`docs/proposals/decentralised-ci-runner-architecture.md`) produced by a parallel-agent workflow, and ADR-049 (Proposed) recommending a tiered split — push the expensive `lake build` elaboration onto the contributor (which `prove_local_verify` already runs) and keep a mandatory cheap central kernel re-check, which re-elaborates the changed-module closure from canonical source rather than trusting any client artifact, as the sole load-bearing soundness gate.
