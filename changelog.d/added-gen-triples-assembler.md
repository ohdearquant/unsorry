Added `tools/sourcing/gen_triples.py` ([ADR-059](docs/adrs/ADR-059-Contributor-Goal-Sourcing-Skill.md) / [SPEC-059-A](docs/adrs/specs/SPEC-059-A-Contributor-Goal-Sourcing-Skill.md)): a goal-triple assembler that turns one validated sourcing candidate into the three on-disk files — the `goals/<slug>.lean` sorry-stub, the SPEC-003-A `goals/<slug>.aisp` record (`status≜open`, `sha≜∅`), and the `backlog/<slug>.md` entry — and (with `--validate`) re-runs Gate B over them. Previously this triple was hand-authored; the tool makes it deterministic and Gate-B-clean, refuses to clobber an existing goal (ADR-018 immutability), and is the assembler the forthcoming `unsorry-goal-sourcing` skill invokes.
