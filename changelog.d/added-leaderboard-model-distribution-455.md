Leaderboard **Model distribution** section (`docs/leaderboard.html`): a bar chart of verified proofs per solver model (provider/model cohort, ADR-023), shown after the contributor list with each model's proof count and share. The generator now exposes a trimmed `models` array in `docs/metrics/leaderboard-ui.json` (`tools/leaderboard`); the page hides the section when no model has solved a proof.
