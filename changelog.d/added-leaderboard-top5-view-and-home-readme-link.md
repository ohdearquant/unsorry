The leaderboard page gains a **Top 5** view: the view switcher is now three tabs (Leaderboard · Top 5 · Proofs over time), where Top 5 shows just the five highest-scoring contributors using the same bar styling as the full board. The home page (`docs/index.html`) now links the repository README (and the GitHub repo) so visitors can read about the project. Both pages are tuned for small screens — the view switcher scrolls horizontally rather than overflowing on narrow viewports.
