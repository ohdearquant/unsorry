The OpenAI `--prove` path now drives proof-specialised models that emit Lean as **text** rather than as function calls: when the tool loop ends without the target module having been written, the harness extracts the Lean module from the model's final answer (a fenced ```lean block, any fenced block, or the trimmed content) and writes it to the target — so models like Leanstral behind an OpenAI-compatible endpoint (`-pi`, ADR-025) can prove, not just translate. Gate A re-checks every extracted proof, so soundness is unchanged (ADR-041, ADR-006). The path also now exits non-zero on a real HTTP/transport failure instead of masking it as an empty answer, so the infrastructure-failure guard (ADR-016) classifies it correctly.
