A new **unsorry Roadmap** GitHub Project mirrors the repo's planning state: every ADR appears as an item carrying its `adrs.json` status (the `Proposed` ones surface as the pending work-ahead) and repo issues are linked live. The `tools.project_sync` tool and `project-sync.yml` workflow keep it reconciled from the source of truth (ADR-077), never clobbering the hand-curated Roadmap Stage lane.
