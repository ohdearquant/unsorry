Added a curated **Proof Showcase** page (`docs/showcase.html`) that communicates what the swarm has achieved to mathematicians and prospective contributors — previously undocumented anywhere. It features three flagship milestones (Nicomachus's identity, the first result proved absent from mathlib; the triangular closed form proved by *reusing the swarm's own* lemma; the Platonic–Schläfli core closed through a 13-lemma decomposition tree) plus a selected-results gallery across number theory, closed forms, and telescoping sums/products. Every theorem statement is copied verbatim from its proof module and every link verified against the repo; the intro explains the kernel-verified soundness guarantee. Linked from the docs site nav (all pages), an index.html feature card, and one README line — no content inlined in README.
