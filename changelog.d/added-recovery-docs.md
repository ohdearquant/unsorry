Added `docs/recovery.md` and a **Recovery** section to the README documenting how the swarm submits proofs through the governor-metered queue, why a proof can get stranded (the direct-submission cutover + Gate A flood, #1904/#1909), and the self-healing recovery pipeline: the admission re-route offer, `reroute_stranded.py` (re-package without re-proving), the scheduled `queue-dispatcher`, and the `close-superseded` sweep — plus the operator quick-reference and the Gate A capacity backstop (ADR-058).
