The visualiser now also emits a **README preview SVG** (`docs/proof-graph.svg`, `tools.visualiser --svg`), mirroring `docs/leaderboard.svg` for two-surface parity (ADR-038): a proof-graph card in the shared visual language showing the per-status goal breakdown and decomposition-family count, linked from the README beside the leaderboard preview. Deterministic from goal statuses alone (git-independent), so it never drifts on a shallow checkout, and it joins the `--write`/`--check` set.
