Cut proof archive block **0003** (ADR-041/SPEC-041-A §8): retired 30 proved goals into `packages/unsorry-archive-0003/` — **whole decomposition trees + standalone goals only**. The active library drops from 108 to 78 modules, shortening Gate A's full-replay/audit long pole (the bottleneck on memory-bound runners, ADR-047). Decomposition records move into the package only when their entire tree (parent + all subs) is archived; split trees are deferred (kept active) so neither the active tree nor the package has a dangling `src` (Gate B GB008/GB016). Goal-`.lean` removals pass the ADR-018 immutability gate via the archive-aware exemption. Boards regenerated. (Block is 30, not the full 40, because the report-only planner is not yet decomposition-tree-aware; cutting whole trees is correct over hitting an exact count.)
