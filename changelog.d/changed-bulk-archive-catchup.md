Catch-up bulk archive sweep: cut blocks `unsorry-archive-0020` through `-0027` (310 proofs), draining the active proved-not-archived set from **322 back to 12**. The active set had ballooned because an archive PR (#2472) was stuck in Gate A for ~5h — the auto-archive cuts one block at a time and skips while an archive PR is open, so the hourly runs no-op'd while proofs kept merging, regrowing the active library toward the slow-Gate-A regime the earlier sweep fixed. This restores the small active set; auto-archive (ceiling 20) maintains it going forward now that the Gate A janitor false-kill is fixed. Archived proofs are validated by provenance + packaging, not re-replayed (ADR-048); goal statements are unchanged (ADR-018 immutability check passes — solved goals are removed as their proofs move into packages).
