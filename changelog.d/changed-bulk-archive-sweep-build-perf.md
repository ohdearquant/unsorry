Bulk-archived the active proof backlog into blocks 0008–0019 (445 proofs), shrinking the active library from **457 modules to ~12**. The Gate A library build (`lake build UnsorryLibrary --wfail`, run in every prepare/audit/replay job) scales with the active-module count — measured at ~233 s for 457 modules with a warm cache — so draining the active set cuts the build (and audit/replay) to seconds. Archived proofs are validated by provenance + packaging, not rebuilt or replayed (ADR-048), so they leave the build entirely. SPEC-041-A §10 now documents the build-performance rationale and the bulk-sweep procedure (loop `tools.archive.apply` until no eligible whole-tree remains, validate per §8 step 5, open one retire PR); the auto-archive ceiling (20) maintains the small active set going forward.
