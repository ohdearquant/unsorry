Unified the docs-site branding and page titles so the pages no longer look stitched together. Every page now uses the lowercase **unsorry** wordmark as its `<h1>`, a consistent `<title>` of the form `unsorry — <Page>` (Home is just `unsorry`), and the same brand-`<h1>` + section-badge header pattern — the Proof showcase page, which previously led with its page name in a smaller heading and a narrower card, now matches the others (brand wordmark, `Proof showcase` badge, `max-w-6xl`). Also gave the leaderboard page the shared `font-sans` body class it was missing, lowercased the wordmark in the generated leaderboard / proofs-over-time / proof-graph OG-card SVGs, and normalised the README story heading to an em-dash. Generated pages were fixed at their source (`tools/queue_board`, `tools/visualiser`, `tools/leaderboard`) and in their committed output in lockstep; the shared nav list already lives in `tools/site_nav.py` (ADR-038/066) and is unchanged.
