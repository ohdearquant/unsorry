Leaderboard fixes (`docs/leaderboard.html`): (1) the `pts` value no longer overlaps the contributor's name on low-score rows — it now renders **after** the bar (left-to-right into open space) instead of right-aligned inside a short bar; (2) rows are now **sorted by points (score) descending** so the order matches the bar lengths and the `pts` labels, with rank = position in that order. NOTE: this makes the *displayed* order points-first, which diverges from SPEC-023-A's documented canonical rank (verified proofs, then difficulty points) — the underlying `score`/`rank` data is unchanged; only the view sorts by points.
