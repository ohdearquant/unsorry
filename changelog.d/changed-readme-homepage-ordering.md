Reorganised the README (the project homepage at unsorry.agentics.org.nz) into four logically-grouped sections so it reads top-to-bottom instead of jumping between topics: **orientation** (What this is → the 10-days story → Why this matters → The goal, honestly), **how it works** (the loop → Design → CI gates → statement fidelity → repository layout → recovery), **join** (Running an agent → Contributing), and **reference** (Roadmap → References → team → license). The story is promoted to a top-level section, "Why this matters" now precedes the design/mechanics, the "Running an agent" call-to-action is no longer buried below the deep internals, and the ephemeral dated "Current status" block moves from the very top to the bottom. Pure reorder — all existing prose is preserved verbatim (verified line-for-line); only section order, the story's heading level, and thematic dividers changed.
