Replaced the README's auto-updated **proof graph** preview image with a **proofs-over-time** line graph — cumulative kernel-verified proofs plotted by the hour each one merged onto `main`. Like the leaderboard card, it is regenerated by the post-merge/scheduled `leaderboard` workflow (`tools.leaderboard` now emits `docs/proofs-over-time.svg`).
