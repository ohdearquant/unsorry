The **#387 retro-audit** is delivered — `check_triviality --all --write` writes `docs/triviality-audit.{md,json}`, the report-only pass over all `goals/` (ADR-035; never deletes). Delivering it surfaced and fixed a #410 over-match: `decide`'s `failed to synthesize\n  Decidable (∀ …)` (the tactic cannot decide a ∀ over an infinite type, inside `first | … | decide | …`) was being read as a *statement* elaboration error → false `probe-error`s under audit load. `classify` now strips that decide-noise before the elaboration-error check; a genuine statement typeclass gap (`failed to synthesize instance`) still surfaces. Closes #387.
