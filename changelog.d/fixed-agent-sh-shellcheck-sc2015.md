Fixed `agent-lint` failing on `main` (and therefore on every open PR): CI's bare `shellcheck` flags SC2015 (`A && B || C` is not if-then-else) on the #612 binding-suppression check in `swarm/agent.sh`. The logic was correct, but the `[ … ] && [ … ] && [ … ] || { … }` chain tripped CI's (older Ubuntu) shellcheck even though newer local builds don't emit it. Rewrote it as an explicit `if ! { … }; then … fi`, which no shellcheck version flags. No behaviour change; `--self-test` green (62 tests).
