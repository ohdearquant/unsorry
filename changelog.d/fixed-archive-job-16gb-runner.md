The `gate-a-archive` job now runs on the 16 GB `namespace-profile-unsorry-2` runner instead of `detect.profile`, which routed non-infra PRs (like archive-cut PRs) to the 8 GB `unsorry-1` runner. Archive validation leanchecker-replays whole blocks — each archive package loads its own full mathlib environment (>8 GB resident) — so it OOM-killed (exit 137) on the 8 GB runner even at a 12-module chunk (#764). unsorry-2 is the same 16 GB class the full active replay already uses. (swapon is blocked on Namespace, so the memory must come from the runner profile.) Also corrected a stale comment that claimed both profiles were 16 GB.
