Corrected leaderboard contributor attribution: solves recorded under the secondary handles `perttu-mp` (explicit `solver≜`) and `OceanLi` (git-add-author-inferred) are now credited to their canonical contributors `@perttu` and `@ohdearquant`. `perttu-mp`'s proof and its three telemetry runs — all on the same `oma-2-c50d` agent as @perttu — fold into @perttu, and the `OceanLi` / `quantocean.li@gmail.com` git identity now maps to @ohdearquant in `contributor-aliases.json`. The two secondary handles no longer appear as separate leaderboard rows.
