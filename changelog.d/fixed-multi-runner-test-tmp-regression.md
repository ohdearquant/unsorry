Fixed `test_claim_agent_identity_multi_runner` (added in the runner-identity lock change) breaking `agent-lint` on Linux CI — it created a bare `mktemp -d` outside the suite sandbox and cleaned up with manual `rm -rf`, which cascaded into "No such file or directory" failures for every later self-test that nests under `$SESSION_TMP`. The test now nests its scratch dir under `$SESSION_TMP` (removed by the suite's EXIT trap, so no manual cleanup) and reaps its background helper process. Pure test hygiene; the runner-identity behaviour is unchanged.
