Fixed three site front-end glitches: the **Showcase** link now appears in the shared top-nav on every page (it was only on the hand-authored Home/Leaderboard/Showcase shells, missing from the generated Queue and Proof-graph pages); the top-nav now **wraps to a second line on narrow viewports** instead of spilling past the card edge on mobile; and on the leaderboard, a contributor's badge row (including the optional **dispatch** badge) now wraps within the name column instead of **overlapping their score bar**.
