# Tasklist: Historical Leaderboard Attribution Recovery

Created: 2026-06-14 02:10:18 UTC

## Scope

Complete the follow-up attribution recovery work for the leaderboard. The final state should preserve strict `solver≜...` proof-provenance ranking while adding a separate historical proof-index contributor view derived from git add-author history.

## Tasks

### LBAR-001: Add Git Attribution Model

Status: claimed by Codex, completed

Implement deterministic git add-author collection for `library/index/*.aisp` proof records. The model must preserve solver provenance separately, record historical attribution as visibility data only, and handle missing git metadata with a clear deterministic fallback or error.

Acceptance:

- `tools/leaderboard/generate.py` can aggregate add-author counts for proof index files.
- Historical attribution does not overwrite or populate `Proof.solver`.
- The implementation can be tested without relying on the real repo history.

### LBAR-002: Add Reviewed Contributor Alias Mapping

Status: claimed by Codex, completed

Add a small reviewed alias source so git author identities can become GitHub profile/avatar links only when explicitly mapped.

Acceptance:

- A repo-local alias file exists under `docs/metrics/`.
- Mapped authors get GitHub profile/avatar URLs in generated UI data.
- Unmapped authors still render as display-only rows without broken links.
- Raw git emails are not required in the browser UI rows.

### LBAR-003: Generate Attribution Gaps Report

Status: claimed by Codex, completed

Generate a machine-readable report of proof index files missing explicit solver provenance, including git add-author identity and alias-map status.

Acceptance:

- `python3 -m tools.leaderboard --write .` writes `docs/metrics/attribution-gaps.json`.
- `python3 -m tools.leaderboard --check .` detects drift in the gaps report.
- The report can be used for later manual review without changing source proof records.

### LBAR-004: Extend UI JSON Contract

Status: claimed by Codex, completed

Extend `docs/metrics/leaderboard-ui.json` with separate historical contributor rows and summary counts.

Acceptance:

- `contributors` remains the ranked solver-provenance leaderboard.
- `historical_contributors` is separate and clearly marked as git-attribution visibility.
- Summary includes git-attributed index counts and historical contributor counts.

### LBAR-005: Update Markdown and SVG Views

Status: claimed by Codex, completed

Update generated `docs/leaderboard.md` and `docs/leaderboard.svg` so the README and markdown views no longer imply the project has only one contributor.

Acceptance:

- Markdown includes a separate historical proof index contributors section.
- SVG summary mentions historical proof authors or git-attributed proof index files.
- SVG remains GitHub README compatible.

### LBAR-006: Update HTML Leaderboard

Status: claimed by Codex, completed

Update `docs/leaderboard.html` to render the historical contributor section from generated UI JSON while keeping solver-provenance rows as the main ranked leaderboard.

Acceptance:

- HTML validates the new JSON shape.
- Historical contributors render separately from ranked solver rows.
- The page has sensible empty/error states.
- No mock leaderboard data is reintroduced.

### LBAR-007: Update Tests

Status: claimed by Codex, completed

Add focused tests for historical attribution, alias mapping, gaps output, UI JSON shape, generated-artifact drift checks, and HTML contract markers.

Acceptance:

- `python3 -m pytest tools/leaderboard -q` passes.
- Tests prove git attribution is separate from solver provenance.

### LBAR-008: Regenerate Artifacts and Validate

Status: claimed by Codex, completed

Regenerate generated leaderboard artifacts and run the relevant validation commands.

Acceptance:

- `python3 -m tools.leaderboard --write .` completes.
- `python3 -m tools.leaderboard --check .` passes.
- `python3 -m pytest tools/leaderboard -q` passes.
- `python3 -m tools.gate_b validate .` passes or any unrelated failure is recorded.

### LBAR-009: Update Leaderboard Skill

Status: claimed by Codex, completed

Update `Skills/unsorry-leaderboard-integration` so future agents understand strict solver credit, historical git attribution visibility, alias mapping, and attribution gaps.

Acceptance:

- `SKILL.md` and any needed references describe the two attribution layers.
- The skill points to generated attribution files and validation commands.

## Append-Only Worklog

- 2026-06-14 02:10:18 UTC: Claimed LBAR-001 through LBAR-009 for implementation in this pass.
- 2026-06-14 02:22:00 UTC: Completed LBAR-001 by adding proof path metadata, batched git add-author collection, strict GitHub handle validation, and historical attribution aggregation without populating `Proof.solver`.
- 2026-06-14 02:22:00 UTC: Completed LBAR-002 by adding `docs/metrics/contributor-aliases.json` with reviewed mappings for current proof-index git authors.
- 2026-06-14 02:22:00 UTC: Completed LBAR-003 by generating `docs/metrics/attribution-gaps.json` and wiring it into `--write` / `--check`.
- 2026-06-14 02:22:00 UTC: Completed LBAR-004 by extending `docs/metrics/leaderboard-ui.json` with `historical_contributors` and summary counts for git-attributed index files, historical contributors, and attribution gaps.
- 2026-06-14 02:22:00 UTC: Completed LBAR-005 by regenerating `docs/leaderboard.md` with a separate historical contributor section and updating `docs/leaderboard.svg` summary text to show historical/git-attributed context.
- 2026-06-14 02:22:00 UTC: Completed LBAR-006 by updating `docs/leaderboard.html` to render historical proof index contributors separately from ranked solver rows.
- 2026-06-14 02:22:00 UTC: Completed LBAR-007 by adding leaderboard tests for git add-author attribution, alias mapping, attribution gaps, UI contract shape, generated drift checks, and HTML markers.
- 2026-06-14 02:22:00 UTC: Completed LBAR-008 by regenerating artifacts and passing `python3 -m tools.leaderboard --check .`, `python3 -m pytest tools/leaderboard -q`, `python3 -m tools.gate_b validate .`, `bash -n swarm/agent.sh`, `./swarm/agent.sh --self-test`, JSON parse checks, and `git diff --check`.
- 2026-06-14 02:22:00 UTC: Completed LBAR-009 by updating `Skills/unsorry-leaderboard-integration` and ADR/spec guidance for separate solver-provenance and historical git-attribution layers.
- 2026-06-14 02:27:00 UTC: Follow-up audit tightened skill wording and templates so implemented files are no longer described as proposed and `attribution-gaps.json` is listed in closeout/checklist assets.
- 2026-06-14 02:34:00 UTC: Fixed SVG rendering bug where historical contributors were summarized but not rendered as rows. `docs/leaderboard.svg` now visibly lists the solver-provenance row plus historical proof-index rows for `@cgbarlow`, `@chat-bit-01`, `@binto`, and `@OceanLi`.
- 2026-06-14 02:45:00 UTC: Revised the final UX from two sections to one gamified credited-proof leaderboard. `contributors` now ranks explicit solver credits plus inferred git add-author credits when `solver≜` is missing, while preserving explicit/inferred counts and attribution gaps for audit.
